<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Main app theme: keep it simple, no custom color references -->
    <style name="Theme.RakshakX" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- You can add color items later if you have corresponding resources -->
    </style>

    <!-- Transparent theme for overlay (already referenced in manifest) -->
    <style name="Theme.Transparent" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:colorBackgroundCacheHint">@null</item>
    </style>

    <!-- Fullscreen red alert theme for FraudAlertActivity -->
    <style name="Theme.RakshakX.FraudAlert" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/black</item>
    </style>

    <!-- Fullscreen green safe theme for SafeCallActivity -->
    <style name="Theme.RakshakX.SafeCall" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/black</item>
    </style>

</resources>