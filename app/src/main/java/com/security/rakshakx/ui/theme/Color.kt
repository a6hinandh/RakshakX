package com.security.rakshakx.ui.theme

import androidx.compose.ui.graphics.Color

// ── Background & Surface ──────────────────────────────────────
val Navy900     = Color(0xFF060A14)   // Deepest background
val Navy800     = Color(0xFF0B1120)   // Card background
val Navy700     = Color(0xFF111827)   // Elevated surfaces
val Navy600     = Color(0xFF1A2332)   // Hover / active state
val Navy500     = Color(0xFF1E293B)   // Borders

// ── Primary ── Cyan ────────────────────────────────────────────
val Cyan400     = Color(0xFF00E5FF)   // Primary accent
val Cyan500     = Color(0xFF00BCD4)   // Secondary accent
val Cyan600     = Color(0xFF0088A3)   // Pressed state
val Cyan700     = Color(0xFF006A80)   // Subtle accent

// ── Teal ───────────────────────────────────────────────────────
val Teal400     = Color(0xFF00E5C3)   // Teal accent
val Teal500     = Color(0xFF00BFA5)   // Teal secondary

// ── Status Colors ──────────────────────────────────────────────
val RedCritical = Color(0xFFFF1744)   // Critical threat
val RedHigh     = Color(0xFFFF5252)   // High threat
val RedMuted    = Color(0xFF3D1520)   // Red bg tint

val OrangeWarn  = Color(0xFFFF9100)   // Warning orange
val AmberWarn   = Color(0xFFFFB300)   // Warning amber
val OrangeMuted = Color(0xFF3D2A10)   // Orange bg tint

val GreenSafe   = Color(0xFF00E676)   // Safe / protected
val GreenLight  = Color(0xFF69F0AE)   // Light green
val GreenMuted  = Color(0xFF0D2818)   // Green bg tint

// ── Text ───────────────────────────────────────────────────────
val TextPrimary   = Color(0xFFE8EDF5)
val TextSecondary = Color(0xFF8892A4)
val TextMuted     = Color(0xFF4A5568)
val TextOnCyan    = Color(0xFF00171F)

// ── Glassmorphism / Glow ───────────────────────────────────────
val GlowCyan    = Color(0x3300E5FF)   // 20% cyan glow
val GlowRed     = Color(0x33FF1744)   // 20% red glow
val GlowGreen   = Color(0x3300E676)   // 20% green glow
val GlowOrange  = Color(0x33FF9100)   // 20% orange glow
val CardBorder  = Color(0xFF1E293B)   // Card border
val GlassBg     = Color(0x1AFFFFFF)   // 10% white glass